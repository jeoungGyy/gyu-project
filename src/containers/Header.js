import React from 'react';
import './Header.scss';

const Header = () => {
  return (
    <header className="header">
      <h1>임정규 작업 폴더</h1>
      <p>
        작업 환경: HTML5, CSS, jQuery, ReactJS, ECMAScript, AWS, GitHub, MongoDB
      </p>
    </header>
  );
};

export default Header;
