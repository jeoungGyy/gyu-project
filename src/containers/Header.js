import React from 'react';
import './Header.scss';

const Header = () => {
  return (
    <header className="header">
      <h1>임정규 작업 폴더</h1>
      <p>작업 환경: HTML5, CSS3, jQuery, ReactJS, ECMAScript6, AWS</p>
    </header>
  );
};

export default Header;
